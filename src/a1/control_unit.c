#ifndef _CONTROL_UNIT_C
#define _CONTROL_UNIT_C

#include "memory.c"
#include "instructions.c"

void work() {
    int pc = 0;
    // while (opcode != HALT) {
    //     instruction = memory[pc];
    //     pc++;
    //     execute(instruction);
    // }
}

#endif
